block vars


-
    const title = self.title ? self.title + ' · Kogu Me Lugu!' : 'Kogu Me Lugu!'
    const getPath = path => {
        if (self.locale === self.defaultLocale) {
            return '/' + (path ? path + '/' : '')
        } else {
            return '/' + self.locale + '/' + (path ? path + '/' : '')
        }
    }


mixin markdown(text)
    | !{ self.md(text) }


doctype html
html(lang=self.locale)
    head
        meta(charset='utf-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge')
        meta(name='description', content='')
        meta(name='viewport', content='width=device-width, initial-scale=1')
        title= title

        //- Disable tap highlight on IE
        meta(name='msapplication-tap-highlight', content='no')

        link(rel='apple-touch-icon', sizes='180x180', href='/assets/images/apple-touch-icon.png?v=2')
        link(rel='icon', type='image/png' sizes='32x32', href='/assets/images/favicon-32x32.png?v=2')
        link(rel='icon', type='image/png' sizes='16x16', href='/assets/images/favicon-16x16.png?v=2')
        link(rel='manifest', href='/assets/images/manifest.json')
        link(rel='mask-icon', href='/assets/images/safari-pinned-tab.svg', color='#3fbebc')
        link(rel='shortcut icon', href='/assets/images/favicon.ico?v=2')
        meta(name='apple-mobile-web-app-title', content='Kogu Me Lugu!')
        meta(name='application-name', content='Kogu Me Lugu!')
        meta(name='msapplication-config', content='/assets/browserconfig.xml')
        meta(name='theme-color', content='#3fbebc')

        //- Fonts
        link(rel='stylesheet', href='/assets/styles/font-awesome.min.css')
        link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Oswald:300,400,700|Open+Sans:400,600,700&amp;subset=cyrillic')

        //- Stylesheets
        link(rel='stylesheet', href='/assets/styles/main.css')

        script.
            (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
            })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

            ga('create', 'UA-43078028-1', 'auto');
            ga('send', 'pageview');

    body
        .wrap
            include /_templates/header.pug

            block content

            include /_templates/footer.pug

        script(src='/assets/scripts/jquery-3.2.1.min.js')

        block script
        script(src='/script.js')
